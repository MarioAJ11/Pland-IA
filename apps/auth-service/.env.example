# ============================================
# AUTH SERVICE - VARIABLES DE ENTORNO
# ============================================
# Copia este archivo a .env y ajusta los valores
# NUNCA subas el archivo .env al repositorio

# ============================================
# BASE DE DATOS (PostgreSQL)
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=plandiadb
DB_USER=postgres
DB_PASSWORD=mysecretpassword
DB_SCHEMA=auth_schema

# ============================================
# JWT CONFIGURATION
# ============================================
# IMPORTANTE: Cambia estos secretos en producción
# Genera secretos seguros: openssl rand -base64 64
JWT_SECRET=CAMBIAME_POR_UN_SECRET_SEGURO_DE_AL_MENOS_32_CARACTERES
JWT_ISSUER=PlandIA.AuthService
JWT_AUDIENCE=PlandIA.Clients

# Duración en minutos del Access Token (recomendado: 15-60)
JWT_ACCESS_TOKEN_MINUTES=15

# Duración en días del Refresh Token (recomendado: 7-30)
JWT_REFRESH_TOKEN_DAYS=7

# Duración en días del Refresh Token con "Remember Me" (recomendado: 30-90)
JWT_REFRESH_TOKEN_REMEMBER_ME_DAYS=30

# ============================================
# CORS CONFIGURATION
# ============================================
# Origenes permitidos separados por coma
# Ejemplo producción: https://pland-ia.com,https://app.pland-ia.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://localhost:1420

# ============================================
# LOGGING
# ============================================
# Niveles: Verbose, Debug, Information, Warning, Error, Fatal
LOG_LEVEL=Information

# Ruta de logs (relativo al ejecutable)
LOG_PATH=logs/auth-service-.log

# Días de retención de logs
LOG_RETENTION_DAYS=30

# ============================================
# SERVER CONFIGURATION
# ============================================
# Puerto del servicio (default: 5012)
ASPNETCORE_URLS=http://localhost:5012

# Entorno: Development, Staging, Production
ASPNETCORE_ENVIRONMENT=Development
